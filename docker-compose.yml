services:
  api-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sharif-funding-api-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3000
    tty: true